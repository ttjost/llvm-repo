//=== HMCVEXInstPrinter.h - Convert HMCVEX MCInst to assembly syntax -*- C++ -*-==//
//
//                     The LLVM Compiler Infrastructure
//
// This file is distributed under the University of Illinois Open Source
// License. See LICENSE.TXT for details.
//
//===----------------------------------------------------------------------===//
//
// This class prints a HMCVEX MCInst to a .s file.
//
//===----------------------------------------------------------------------===//

#ifndef HMCVEXINSTPRINTER_H
#define HMCVEXINSTPRINTER_H

#include "llvm/MC/MCInstPrinter.h"

#include "llvm/Support/Debug.h"
#include "llvm/Support/ErrorHandling.h"
#include "llvm/Support/raw_ostream.h"
#include <map>

#define DEBUG_TYPE "HMCVEX-instprinter"

namespace llvm {
    
    class TargetMachine;
    
    class HMCVEXInstPrinter : public MCInstPrinter{

        bool EnableVLIWScheduling;

        std::map<StringRef, bool> ExtFuncToPrint;

    public:
        HMCVEXInstPrinter(const MCAsmInfo &mai, const MCInstrInfo &mii,
                       const MCRegisterInfo &mri)
            : MCInstPrinter(mai, mii, mri), EnableVLIWScheduling(true) {
            }
        
        // Autogenerated by tblgen.
        void printInstruction(const MCInst *mi, raw_ostream &O);
        static const char *getRegisterName(unsigned RegNo);
        
        void printRegName(raw_ostream &O, unsigned RegNo) const override;
        void printInst(const MCInst *mi, raw_ostream &O, StringRef Annot,
                       const MCSubtargetInfo &STI) override;
        
        bool printAliasInstr(const MCInst *mi, raw_ostream &OS);
        void printCustomAliasOperand(const MCInst *mi, unsigned OpIdx,
                                     unsigned PrintMethodIdx, raw_ostream &O);
        
    private:
        void printOperand(const MCInst *mi, unsigned OpNo, raw_ostream &O);
        void printUnsignedImm(const MCInst *mi, int opNum, raw_ostream &O);
        void printMemOperand(const MCInst *mi, int opNum, raw_ostream &O);
        
        void printMemOperandEA(const MCInst *MI, int opNum, raw_ostream &O);
        
        void printReturnDirective(const MCInst *MI, raw_ostream &O);
        void printCallDirective(const MCInst *MI, raw_ostream &O);
        
    };
}


#endif
